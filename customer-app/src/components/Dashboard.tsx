import { Typography } from '@mui/material';

const Dashboard = () => {
  return (
    <div>
      <Typography variant="h4">Welcome to Customer CRUD</Typography>
      
    </div>
  );
};

export default Dashboard;